services:
  - type: web
    name: sistema-geral-copal-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.19.3
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase:
          name: sistema-geral-copal-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 8h
      - key: REFRESH_TOKEN_EXPIRES_IN
        value: 7d
      - key: UPLOAD_PATH
        value: uploads
      - key: CORS_ORIGINS
        value: http://localhost:3000,http://127.0.0.1:3000,https://2001diegodeoliveira-svg.github.io,https://sistema-geral-copal.vercel.app
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: 587
      - key: SMTP_SECURE
        value: false
      - key: SMTP_USER
        value: sespprojeto@gmail.com
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        value: sespprojeto@gmail.com

# Banco PostgreSQL gerenciado pelo Render
databases:
  - name: sistema-geral-copal-db
    plan: free
    databaseName: copal
    user: copal_user
